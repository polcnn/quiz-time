import { green, gold, red, gray, blue } from "@ant-design/colors";

export const colors = {
  success: green.primary,
  warning: gold.primary,
  error: red.primary,
  secondary: gray.primary,
  info: blue.primary,
};
